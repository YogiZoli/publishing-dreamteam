fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
pydantic-settings>=2.3
httpx>=0.27
asyncpg>=0.29
cryptography>=42.0
authlib>=1.3
itsdangerous>=2.2
python-multipart>=0.0.9
# Real timed captions. The old video.google.com/timedtext endpoint this app
# used is dead — it returns HTTP 200 with 0 bytes for every video, listed or
# not (probed 2026-07-21), which is why every pack had estimated chapters.
youtube-transcript-api>=1.0
